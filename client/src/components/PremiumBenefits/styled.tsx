import styled from 'styled-components';
import {Container, Row, Col} from 'react-bootstrap';

export const BenefitsBackground = styled.div.attrs(() => ({
  className: 'py-5',
}))`
  background-color: #f9fbfd;
`;

export const BenefitsContainer = styled(Container).attrs(() => ({
  className: 'my-5 py-5',
}))``;
